@extends('layouts.admin')
@section('content')
<form method="POST" action="{{ $household->exists ? route('admin.households.update',$household) : route('admin.households.store') }}" class="max-w-2xl space-y-5 rounded-lg border bg-white p-6">@csrf @if($household->exists)@method('PUT')@endif<h1 class="text-2xl font-bold">{{ $household->exists?'Ubah KK':'Tambah KK' }}</h1><label class="block text-sm font-medium">Nomor KK<input @if(!$household->exists) required @endif name="kk_number" value="{{ old('kk_number',$household->exists ? '' : $household->kk_number) }}" placeholder="{{ $household->exists ? '•••• •••• •••• '.substr($household->kk_number,-4).' — isi hanya untuk mengganti' : '' }}" class="mt-1 block w-full rounded border-slate-300" /></label><label class="block text-sm font-medium">Rumah<select name="house_id" class="mt-1 block w-full rounded border-slate-300"><option value="">Belum ditempatkan</option>@foreach($houses as $house)<option value="{{ $house->id }}" @selected((string)old('house_id',$household->house_id)===(string)$house->id)>{{ $house->house_number }} — {{ $house->address }}</option>@endforeach</select></label><label class="block text-sm font-medium">Status<select name="status" class="mt-1 block w-full rounded border-slate-300"><option value="active" @selected(old('status',$household->status)==='active')>Aktif</option><option value="inactive" @selected(old('status',$household->status)==='inactive')>Tidak aktif</option></select></label><button class="rounded bg-teal-700 px-4 py-2 text-sm font-medium text-white">Simpan</button></form>
@endsection
